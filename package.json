{
  "name": "akindukogroup",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run generate-manifest && next dev --turbopack",
    "build": "npm run generate-manifest && next build",
    "start": "next start",
    "generate-manifest": "node scripts/generate-manifest.js",
    "lint": "next lint"
  },
  "dependencies": {
    "fs-extra": "^11.2.0",
    "glob": "^11.0.0",
    "next": "15.1.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "sass": "^1.83.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "eslint": "^9",
    "eslint-config-next": "15.1.2"
  }
}
